spring:
  application:
    name: abms
  jpa:
    hibernate:
      ddl-auto: create
  docker:
    compose:
      lifecycle-management: start_only
  threads:
    virtual:
      enabled: true

logging:
  level:
    p6spy: debug
