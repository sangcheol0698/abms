global:
  scrape_interval: 15s

rule_files:
  - /etc/prometheus/alerts/abms-alerts.yml

scrape_configs:
  - job_name: abms-api
    metrics_path: /actuator/prometheus
    static_configs:
      - targets:
          - __ABMS_API_TARGET__
